import { CartInfo, Container, DiscountContainer } from "../../components/Client";

function CartDetail() {
	return (
		<Container>
			<DiscountContainer />
            <CartInfo />
		</Container>
	);
}

export default CartDetail;
